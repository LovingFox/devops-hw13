---
# vars file for builder
project_name: devops-hw13
instance_name: builder
hostname_file: "{{ instance_name }}-hostname"
ansible_ssh_private_key_file: "./{{ instance_name }}-private.key"
ansible_ssh_user: ubuntu

remote_repo: "https://github.com/boxfuse/boxfuse-sample-java-war-hello.git"
repo_dir: "/root/boxfuse"
target_file: "{{ repo_dir }}/target/hello-1.0.war"
